# T-037 — proxy-rules-simplify-and-port-fix · 用户输入

> PM Orchestrator 接收用户原始请求（中文）；后续 stage 文档以此为权威输入。

## 用户描述

> 1. 批量配置的规则在 webUI 上点击"展开明细"无反应，也就无法管理这些代理规则；理论上批量添加的，应该就是一次添加多条单个规则；不要批量添加功能了吧，就只要单个添加功能；
> 2. 添加单个规则时，我设置远程端口为 10022，保存后，webUI 上显示远程端口为 223，需要修复；
> 3. 去掉自动探测端口的功能，端口由人工管理；

## 用户决策授权

> "以用户体验好、符合软件工程标准、长期易使用易维护为原则来决策；你来决策就可以了，我只看结果是否符合需求……所有 commit 和 push 都由你来操作。"

- PM 全权决策：架构选型、是否同步删除相关测试/OpenAPI/dev-map、是否保留端口预设标签（属"快速填充"而非"自动探测"）。
- 不再向用户求确认；遇到冲突时按"用户体验好 + 长期易维护"取舍。

## Mode

`full` —— 涉及前后端 + 测试 + 文档；UX 简化属重大行为变更，必须走 7-stage 完整管道。

## 关联历史

- **T-018**（upload-bin-multiport-ip-probe，归档）—— 本次要删除的"批量创建 / 端口探测 / 折叠分组"功能的引入者。
- **T-032**（proxy-form-vmodel-oom-fix，归档）—— ProxyForm 单向数据流（`initialValue` prop + `defineExpose getProxyInput()`）约定；本任务**必须保留**该单向数据流契约，不得回退到 v-model 双向桥（insight L28）。
- **T-027**（download-cancel-and-upload-decouple，归档）—— "互斥的 409 错误必须给用户明确路径解锁"模式（insight L16）；本任务删除批量功能后，相关 409 文案需同步清理。
