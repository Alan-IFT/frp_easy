# 任务看板 — frp_easy

> 由 **PM Orchestrator** 维护。每个任务开始时记录到这里，并随生命周期更新。
>
> 新任务开始前，应该查这个看板找相关历史工作。

## 进行中

| ID | Slug | 阶段 | 模式 | 文档目录 |
|---|---|---|---|---|

## 已完成

| ID | Slug | 结果 | 完成 | 文档目录 |
|---|---|---|---|---|
| T-067 | responsive-layout | DELIVERED（mode: full；批次 ux-ui-uplift-2026-05 第 6/最后一个，批次收尾；dev-frontend 单分区，纯前端；0 rollback；verify_all 全量真跑 PENDING 待 batch orchestrator Bash 会话执行作硬闸门，特别复核 e2e C.1 03-dashboard 1280 视口保持展开 + AppLayout 既有 8 例 + Server/Client 既有 spec 零回归 + frontend_tests==600 / go_tests==342 / test_count==942） | 2026-05-31 | `docs/features/responsive-layout/`（pending archive；按批次约定未 commit/未 archive，由 batch orchestrator 统一处理。让应用外壳窄屏/移动端可用——支撑 FRP 管理面板"手机查看进程/重启穿透"真实场景。新建模块级单例 composable web/src/composables/useViewport.ts（复刻 T-066 useTheme 范式，不抽公共 util 避免反向耦合 theme/viewport）：原生 window.matchMedia('(max-width:767.98px)')→isNarrow:Ref<boolean>；NARROW_MAX_WIDTH=767.98 让 768 整数判展开（BC-1 <768 折叠/>=768 展开）且 767.98<1280（e2e Desktop Chrome 视口）→e2e 默认视口恒判展开 03-dashboard 零回归（BC-2）；safeMatchMedia null/抛错降级保持 false=展开（BC-6）；initialized 守卫 change 监听只注册一次（NFR-4 无泄漏 BC-5 抖动安全）；addEventListener+addListener 双试老 Safari 兼容。AppLayout.vue 消费：collapsed=ref(isNarrow.value) 初值=断点默认态（FR-1）+ watch(isNarrow) 非 immediate 仅跨 768 阈值触发（FR-3）；手动 show-trigger（@expand/@collapse）共存——同区间手动展开 isNarrow 未变 watch 不触发 collapsed 保持用户值不被强制收回（FR-2 不锁死核心论证）；顶栏 n-layout-header height→min-height:56px + n-space :wrap=true（FR-4 优雅换行）+ 版本号 n-text 加 !isNarrow 窄屏隐藏（FR-4/5）；内容区 padding 24px→isNarrow?12:24px（FR-8 可选）。Server.vue 5 处 + Client.vue 3 处 style='width:Npx'→'width:100%; max-width:Npx'（FR-6/7），max-width 封顶使鉴权 Token 行 input 不占满整行'查看明文'按钮仍并排（C-2）。不改菜单结构/路由/activeKey/menuIcon a11y（T-064）/主题 n-select（T-066）/退出按钮文本位置（e2e TC-05 保护）/横幅逻辑（OOS-3）；App.vue 不改（OOS/设计§10）；不引新颜色（OOS-5）。零新依赖（matchMedia 原生，未用 naive-ui useBreakpoint 因分界点 640/1024 不对齐 768 边界方向不透明）。+24 前端测试（frontend_tests 576→600 / test_count 918→942 / version 34；go_tests 不变 342；dev 18 + QA 独立对抗 6）：useViewport.spec.ts 新建 9（窄/宽初值 2 + 跨阈值 change 响应式 2 + 模块单例同引用/监听守卫 listeners.length===1 2 + 无 matchMedia/抛错降级不崩 2 + NARROW_MAX_WIDTH<1280 边界 1）+ AppLayout.spec.ts +5（窄屏初始折叠/宽屏初始展开/宽→窄自动折叠/窄→宽自动展开 用 .n-layout-sider--collapsed class 可观察量 + 顶栏关键入口主题切换/退出登录始终存在）+ Server.spec.ts +2（max-width 正向 + 反向证伪无裸像素宽残留）+ Client.spec.ts +2（同上）+ qa_t067_adversarial.spec.ts 新建 6 QA 独立对抗（QA-ADV-1 窄屏折叠 / QA-ADV-2 宽屏展开桌面不回归 / QA-ADV-3 窄屏折叠后手动 trigger 展开后视口仍窄 emit(true) 不被强制收回不锁死最高风险 AC-3 + trigger DOM 不稳回退等价命题 / QA-ADV-4 宽→窄→宽往返不残留 / QA-ADV-5 窄屏顶栏主题切换+退出可达 + 折叠态 7 menuIcon aria-label a11y 不退化 / QA-ADV-6 1280 等价 matches=false 侧栏展开菜单文本'仪表盘'可见 + 常量<1280 双保险反向证伪 e2e 折叠回归）。测试策略：useViewport/AppLayout/qa 对抗用 vi.stubGlobal('matchMedia', 受控 FakeMql/QaMql(matches 可控+暴露 change 回调 fire/emit 模拟视口跨阈值)) + vi.resetModules() + 动态 import 全新模块单例规避 isNarrow/initialized 跨用例泄漏（复刻 useTheme.spec C-1）；既有 AppLayout 8 例不动——不 stub matchMedia 时 happy-dom 默认对 (max-width:767.98px) 返 false→isNarrow=false→展开零回归；Server/Client 既有 spec 经 grep 无 width/max-width 断言→max-width 改动透明零回归（insight L37 实读断言判别）。断言全用可观察量（isNarrow 布尔/.n-layout-sider--collapsed class/inline style 含 max-width+width:100%/DOM aria-label/文本），少用 naive-ui 组件名查询（insight L45）。改动仅 web/src/composables/useViewport.ts(新)+web/src/components/AppLayout.vue+web/src/pages/{Server,Client}.vue+web/src/composables/__tests__/useViewport.spec.ts(新)+web/src/components/__tests__/{AppLayout.spec.ts(编辑),qa_t067_adversarial.spec.ts(新)}+web/src/pages/__tests__/{Server,Client}.spec.ts(编辑)+baseline(同时修复既有冗余尾巴为合法单一 JSON C-4)+dev-map，未碰后端/store/路由/API/DB/Go/e2e spec/App.vue/LogViewer/package.json。e2e 不受影响（insight L34：PM/CR/QA 三轮 grep 核实 03-dashboard TC-04 getByText('仪表盘').first()+frpc/frps 卡片文本、TC-05 按 name '退出登录' 点击；playwright devices['Desktop Chrome'] 视口 1280≥768 阈值保持展开；01-setup/02-auth 无布局断言）。0 rollback；GR APPROVED FOR DEVELOPMENT（8 维全 PASS，0 WARN/0 FAIL，5 开发期条件 C-1~C-5 落实）；CR APPROVED 一次过（0 CRITICAL/MAJOR/MINOR，2 NIT）；QA APPROVED FOR DELIVERY 0 缺陷；06 含裸 ## Adversarial tests（6 条独立反向证伪）；07 含裸 ## Insight（4 条：视口断点宜模块单例 composable+原生 matchMedia 边界可控优于 useBreakpoint / 自动折叠'尊重手动'用非 immediate watch 仅跨阈值触发不可 immediate 否则锁死 / 自动折叠阈值必须<e2e 视口宽度否则烟雾测试假性失败 / 表单 width:100%+max-width 桌面不回归且并排兄弟元素不被推开）。verify_all 全量真跑因 PM/dev/QA role-collapsed 上下文无 Bash/PS（insight L31）标 PENDING，静态+确定性预测全绿（预期 PASS / frontend_tests==600 / go_tests==342 / test_count==942），交付硬闸门交 batch orchestrator Bash 会话真跑） |
| T-066 | dark-theme-support | DELIVERED（mode: full；批次 ux-ui-uplift-2026-05 第 5 个，最大任务；dev-frontend 单分区，纯前端；0 rollback；verify_all 全量真跑 PENDING 待 batch orchestrator Bash 会话执行作硬闸门，特别复核 LogViewer/ServiceStatusCard/AppLayout 既有 spec 零回归 + frontend_tests==576 / go_tests==342 / test_count==918） | 2026-05-31 | `docs/features/dark-theme-support/`（pending archive；按批次约定未 commit/未 archive，由 batch orchestrator 统一处理。加生产暗色主题：跟随系统 useOsTheme + 手动 light/dark/auto 三态切换 + localStorage 持久化，激活 T-036（日志子系统 useThemeVars）/T-038（ServiceStatusCard useThemeVars）已就绪但全站未启用的主题感知基建（此前 App.vue 裸 NConfigProvider 只能浅色）。新建模块级单例 composable web/src/composables/useTheme.ts（pref('light'|'dark'|'auto') + activeTheme computed(null=浅/darkTheme=暗) + isDark + setPref；THEME_STORAGE_KEY='frpEasy.themePref'；DEFAULT_PREF='auto'；内置 createSafeStorage BC-13 副本复刻 useLogPrefs 范式不抽公共 util 避免反向耦合 log/theme 两域+触动 useLogPrefs 测试 T-061 教训 R-4；osThemeRef 惰性化首次在 App.vue setup 内 useOsTheme()——hook 须 setup 内调用，dark/light/setPref 分支不读 OS osThemeRef?.value null 安全；setPref isThemePref 守卫防非法值）。App.vue 绑 <n-config-provider :theme=activeTheme> + 新增 <n-global-style/>（放 config-provider 子树内，让 body 背景随主题切换=修整页硬编码背景最干净办法）。AppLayout.vue 顶栏"退出登录"按钮前加 n-select 三态（跟随系统/浅色/深色，aria-label='主题切换'，onThemeChange 联合类型收口到 ThemePref，放退出按钮前不改其文本/位置 e2e AC-13 保护）+ 品牌绿 #18a058→themeVars.primaryColor。token 化散落 hex（§2.6 核心集合，GR C-3）：Login/Setup 删整页 background:#f5f5f5（靠 n-global-style 给 body 浅色 themeVars 背景观感等价）、Wizard 删 #f0f2f5 + 品牌 #18a058→primaryColor + 三处角色描述 #888→textColor3 + step3 图标 #f0a020/#18a058→warning/primaryColor、FirewallHint 命令块 #f5f5f5→codeColor、PublicIpDetector advisory #888→textColor3、ServiceStatusCard scoped css .svc-status-card--warn 的 #f0a020（border+inset box-shadow）→ :style computed warnCardStyle 走 themeVars.warningColor（needsFix 时给样式否则空对象=默认边框等价 --ok；删 --warn/--ok scoped 块，复刻同文件 cmdBlockStyle 范式 C-3）。不动 LogViewer 子系统（var(--log-*)←useThemeVars 已就绪自动跟随，OOS-2）。零新依赖（darkTheme/useOsTheme/useThemeVars/NGlobalStyle 均 naive-ui ^2.38.0 内置）。+24 前端测试（frontend_tests 552→576 / test_count 894→918 / version 33；go_tests 不变 342；dev 19 + QA 独立对抗 5）：useTheme.spec.ts 新建 12（AC-1 默认 auto+OS浅→浅不回归 / AC-2 dark→darkTheme+持久化 / AC-3 light 不受 OS 暗影响 BC-6 / AC-5 预置 dark 重载读回 / AC-4 auto+OS暗→dark / auto+OS浅→null / BC-4 OS 运行时切换响应式跟随 / BC-5 useOsTheme null→浅不崩 / BC-2 非法值 purple 降级 / BC-3 缺失默认 auto / BC-1 setItem 抛错内存降级不崩 / setPref 拒绝非法值）+ App.spec.ts 新建 4（含 NGlobalStyle / 默认 auto+OS浅→NConfigProvider theme null 不回归 / 切 dark→theme=darkTheme 响应式 / auto+OS暗→darkTheme）+ AppLayout.spec.ts +3（aria-label='主题切换' 控件存在 / setPref(dark)→pref+localStorage 持久化 / 不改退出登录按钮 AC-13）+ qa_t066_adversarial.spec.ts 新建 5 QA 独立对抗（QA-ADV-1 默认+OS浅+OS null 绝不暗色浅色不回归 / QA-ADV-2 auto 真跟随 OS 暗↔浅响应式无残留 / QA-ADV-3 手选 dark 全新实例模拟重载仍读回不退默认 / QA-ADV-4 getItem+setItem 全程抛错构造+切换均不崩内存降级比 dev BC-1 更狠 / QA-ADV-5 顶级路由页等价场景 NConfigProvider{darkTheme} 包裹子树 useThemeVars 派生暗色 bodyColor≠浅色证明主题 context 穿透）。测试策略：useOsTheme 统一 vi.mock('naive-ui') 受控 osThemeRef（使 useTheme 非 setup 也安全调 + mock OS dark/light/null）；useTheme/App.spec/qa_adv 用 vi.resetModules()+动态 import 拿全新模块单例规避 pref 跨用例泄漏（模块顶层 ref 只首次求值一次的代价，GR C-1）；AppLayout.spec beforeEach setPref('auto')+localStorage.clear+osThemeRef='light' 复位单例。darkTheme 取 importActual 真实对象引用相等断言。断言全用可观察量（localStorage 值/pref/activeTheme===darkTheme/isDark/DOM aria-label/themeVars 派生值），少用 naive-ui 组件名查询（insight L45），App.spec 对 NConfigProvider/NGlobalStyle 结构断言是 AC-8 本质不可避免。改动仅 web/src/composables/useTheme.ts(新) + web/src/App.vue + web/src/components/{AppLayout,FirewallHint,PublicIpDetector,ServiceStatusCard}.vue + web/src/pages/{Login,Setup,Wizard}.vue + web/src/composables/__tests__/{useTheme,qa_t066_adversarial}.spec.ts(新) + web/src/__tests__/App.spec.ts(新) + web/src/components/__tests__/AppLayout.spec.ts(编辑) + baseline + dev-map，未碰后端/store/路由守卫/API/DB/Go/e2e spec/LogViewer 子系统/package.json。e2e 不受影响（insight L34：PM grep 核实 03-dashboard TC-04 断言仪表盘/frpc/frps 文本可见、TC-05 按 getByRole name '退出登录' 点击，n-select 放退出按钮前不改其文本/位置不遮挡；01-setup/02-auth 无主题断言）。0 rollback；GR APPROVED WITH CONDITIONS（8 维全 PASS，0 WARN/0 FAIL，5 开发期条件 C-1~C-5 均落实）；CR APPROVED 一次过（0 CRITICAL/MAJOR/MINOR，2 NIT）；CR flag 的 ServiceStatusCard --warn class 回归风险经 PM 主动 grep 确证伪命题（无该 spec 文件、全 src 无 class 断言，insight L42 体现）；QA APPROVED FOR DELIVERY 0 缺陷；06 含裸 ## Adversarial tests（5 条独立反向证伪：默认浅色不回归 / auto 跟随 OS mock / 手选暗色持久化重载保持 / localStorage 不可用内存降级不崩 / 顶级路由页跟随全局主题）；07 含裸 ## Insight（4 条：暗色主题状态层宜模块单例 composable+osTheme 须 setup 内调用惰性 null 安全 / 测模块单例不同持久化态须 vi.resetModules+动态 import / 顶级路由页不嵌 AppLayout 但仍被根 NConfigProvider 包裹照常跟随主题 context inject 穿透 / scoped css 状态色 token 化最小改法移到 :style computed + 删 class 前必 grep 测试断言）。顶级路由页（/login /setup /wizard）无页内切换入口但跟随全局主题=可接受范围边界（insight L30/L31，OOS-1 显式记录）。verify_all 全量真跑因 PM/dev/QA role-collapsed 上下文无 Bash/PS（insight L31）标 PENDING，静态+确定性预测全绿（预期 PASS / frontend_tests==576 / go_tests==342 / test_count==918），交付硬闸门交 batch orchestrator Bash 会话真跑） |
| T-065 | mapprocerr-sentinel-hygiene | DELIVERED（mode: full；批次 ux-ui-uplift-2026-05 第 4 个；dev-backend 单分区，纯后端；0 rollback；verify_all 全量真跑 PENDING 待 batch orchestrator Bash 会话执行作硬闸门，特别复核 procmgr 既有测试零回归 + handlers_cancel_then_upload_test.go:347 不受影响 + go_tests==342 / test_count==894） | 2026-05-31 | `docs/features/mapprocerr-sentinel-hygiene/`（pending archive；按批次约定未 commit/未 archive，由 batch orchestrator 统一处理。把 internal/httpapi/handlers_proc.go:mapProcErr 对 procmgr 内部错误文本的脆弱 strings.Contains 匹配收口为 procmgr.ErrBusy sentinel + 500 路径走 writeInternalError 不泄露内部文本，对称复刻 T-059（mapProxyWriteError）/ T-055（writeInternalError）。procmgr：manager.go 新增包级 var ErrBusy = errors.New('procmgr: process busy')（同 binloc.ErrBinMissing 范式）+ errors import；Start 的 StateStopping 分支（唯一'忙/过渡态拒绝'返回点，经 RA+SA+dev 三轮通读 grep 确认全集；StateStarting/StateRunning 是 idempotent 不报错故 handler 现行匹配 starting/running 为空匹配）裸 fmt.Errorf 改 fmt.Errorf('procmgr.Start(%s): currently stopping: %w', kind, ErrBusy)（保留可读 cause 进日志 + errors.Is 可判）；OOS-5 不为 starting/running 补忙语义（不改 Start idempotent 可观察行为）；Stop/Restart/ApplyConfigChange 无独立忙返回；生命周期状态机零改动。handler：mapProcErr 由包级函数升级为 *handlers 方法（同包 mapProxyWriteError 既有方法范式），分类改 errors.Is(binloc.ErrBinMissing)→422 BIN_MISSING（保持现状）/ errors.Is(procmgr.ErrBusy)→409 PROC_BUSY 固定中文'进程正忙（启动或停止进行中），请稍后重试'（不透传内部英文）/ else→h.writeInternalError(w,'操作进程失败',err)（500 INTERNAL 固定中文 + 原始 error 进 logger 不外泄）；删除 strings.ToLower+strings.Contains 整块（同时消除对 starting/running 的既有空匹配）+ 删 'strings' import（防 imported-not-used）；调用点 procStart/procRestart 同步改 h.mapProcErr。errors.go 三常量 PROC_BUSY/BIN_MISSING/INTERNAL 值与语义不变（仅复核未改）。+9 Go 顶层测试（go_tests 333→342 / test_count 885→894 / version 32；dev 6 + QA 独立对抗 3）：manager_test.go +3 dev（TestErrBusy_IsSentinel 单/多层 wrap errors.Is+保留可读 cause+无关错误不命中 FR-1/BC-2、TestStart_StoppingReturnsErrBusy 白盒注入 StateStopping 真覆盖 Start 分支 errors.Is(ErrBusy) AC-2 无需真 spawn、TestStart_NonBusyErrorsNotErrBusy no-config-path/bin-missing 不误命中 FR-2 不误纳）+ strings/fmt import；handlers_hygiene_test.go +3 dev（TestMapProcErr_Busy_409_FixedMessage_NoLeak 包 ErrBusy→409+固定中文+不泄露 procmgr/currently stopping/process busy AC-3、TestMapProcErr_Internal_500_NoLeak 非 sentinel→500+固定中文+不泄露+cause 进 logger AC-4、TestMapProcErr_BinMissing_422_Preserved 回归护栏 AC-5）+ fmt/binloc/procmgr import，复用 newCapturingHandlers+捕获 slog（insight L28）；qa_t007_adversarial_test.go +3 QA 独立对抗（TestAdversarial_T065_TextLooksBusyButNotSentinel_Goes500 含 stopping/starting/running 文本但不包 ErrBusy 的错误恒走 500 而非旧 409——运行时+静态双证 strings.Contains 已死 AC-8 核心 insight L34、AnyCauseTextStillBusyIfSentinel cause 改任意字甚至中文只要包 ErrBusy 恒 409+固定中文不随 cause 变、500NoLeakButLogged_BusyNotDowngraded 逐子串不泄露+完整进 logger+ErrBusy 不误降+binMissing 不塌缩 AC-4/NF-1）+ encoding/json/errors/fmt/httptest/binloc/procmgr import。关键（insight L35）：经 mapProcErr 的'断言旧透传行为'既有测试集=空集（RA/SA/GR/CR/QA 五轮 grep 一致确认：procmgr 测试只断言 err==nil/!=nil 从不断言 'currently stopping' 文本；httpapi 无直测 mapProcErr 409/500 message 透传；handlers_cancel_then_upload_test.go:347 是 uploadBin 独立路径=OOS-1 不动零影响）→ 本任务为纯补测锁新意图，无需 PM 批准的过时断言更新（与 T-059 有 Validation_Preserved 需改名改断言不同；PM_LOG 含'为何无需批准'显式判定段）；既有测试零回归。改动仅 internal/procmgr/{manager.go,manager_test.go} + internal/httpapi/{handlers_proc.go,handlers_hygiene_test.go,qa_t007_adversarial_test.go} + baseline + dev-map，未碰前端/storage/DB/migration/binloc 源码/errors.go/handlers_system.go。-race 因本机无 cgo 不跑（与 T-050/T-063 先例一致），并发安全静态论证：本任务不改 procmgr 锁/状态机，仅改'忙'分支错误返回值构造（ps.info.State 赋值零改动），锁顺序不变。0 rollback；GR APPROVED FOR DEVELOPMENT（8 维全 PASS，0 WARN/0 FAIL）；CR APPROVED 一次过（0 CRITICAL/MAJOR/MINOR，2 NIT）；QA APPROVED FOR DELIVERY 0 缺陷；06 含裸 ## Adversarial tests（3 条独立反向证伪：陷阱文本恒 500 / cause 任意变化恒 409 / 500 不泄露但进日志）；07 含裸 ## Insight（收口最强反向证伪是'陷阱文本不再误判 409'而非'忙态仍 409' + 收口未必伴随断言更新空集结论也须 PM_LOG 显式记录判定）。verify_all 全量真跑因 PM/dev/QA role-collapsed 上下文无 Bash/PS（insight L31）标 PENDING，静态+确定性预测全绿（预期 PASS / go_tests==342 / test_count==894 / frontend_tests==552），交付硬闸门交 batch orchestrator Bash 会话真跑） |
| T-064 | menu-icons-and-a11y | DELIVERED（mode: full；批次 ux-ui-uplift-2026-05 第 3 个；dev-frontend 单分区；0 rollback；verify_all 全量真跑 PENDING 待 batch orchestrator Bash 会话执行作硬闸门，特别复核 FirewallHint/PublicIpDetector 既有用例零回归 + 新建 AppLayout/LogList/qa_t064 spec 可挂载 + frontend_tests==552 / go_tests==333） | 2026-05-31 | `docs/features/menu-icons-and-a11y/`（pending archive；按批次约定未 commit/未 archive，由 batch orchestrator 统一处理。修侧边栏菜单图标语义缺陷 + 补三处真实键盘/屏幕阅读器可访问性，零新依赖、零行为/结构/路由改动。核心缺陷：AppLayout menuOptions 中"服务端配置"(:144) 与"设置"(:169) 同用齿轮 ⚙，:collapsed-width=64 折叠态仅图标视觉撞车→误点。IS-1/IS-2：抽内联 helper menuIcon(glyph,name)=h('span',{class:'n-icon',role:'img','aria-label':name,title:name},glyph)，7 个顶层项字形两两互不相同（'设置' ⚙→⚒ 消重复）+ 每 icon span 挂 aria-label+title+role=img 无障碍名（aria-label 主路径/title 悬停/role=img 让 AT 当有名图像不逐字朗读裸字形），取值=label 故 server≠settings；不改菜单结构/路由 key/activeKey 计算（:122-128 /server/monitor 特判不变）。IS-3：LogList.vue 滚动容器 div.log-list-scroll 加 tabindex=0（纯键盘 Tab 聚焦后方向键/PageUp/Down 滚动，overflow-y:auto 原生支持无需显式 keydown，区别于 paused-banner role=button 需 enter/space）+ role=log（GR C-1 裁定默认 log，AC-5 兼容 region 备选）+ aria-label=日志输出，范式对齐同文件 paused-banner :30-41。IS-4：FirewallHint.vue 单条复制按钮+复制全部按钮、PublicIpDetector.vue 复制按钮承载切换文案元素加 aria-live=polite（首渲染不播报、文案'复制'→'已复制 ✓'变化播报 BC-4），仅加 ARIA 不改 copyToClipboard 逻辑/复制行为/文案（T-061 util 不动）。+18 前端测试（frontend_tests 534→552 / test_count 867→885 / version 30；go_tests 不变 333；dev 16 + QA 独立对抗 2）：AppLayout.spec.ts 新建 5（7 图标各非空 aria-label / role=img+title==aria-label / 无障碍名覆盖全部菜单文案 / 7 字形 Set.size==7 反向证伪撞车 / server≠settings 字形+名核心反向证伪）+ LogList.spec.ts 新建 6（tabindex=0 / role∈{log,region}+非空 aria-label / 有日志行用真 parseLogLine 仍带 tabindex+role / AC-6 错误态无滚动容器 / 加载态无滚动容器 / 空态有滚动容器）+ FirewallHint.spec +3 + PublicIpDetector.spec +2（各 aria-live=polite + 行为保真）+ qa_t064_adversarial.spec.ts 新建 2（QA-ADV-1 折叠态撞车两项字形+aria-label 双重可区分 AND ⚙ 全菜单仅出现一次反向证伪历史根因；QA-ADV-2 tabindex+role 同在真实可滚 .log-list-scroll、外层 .log-list-root 不抢 tabindex 反向证伪聚焦落点错位）。断言全用 DOM 属性查询（span.n-icon aria-label/title/role、find('.log-list-scroll').attributes() tabindex/role/aria-label、find('button').attributes('aria-live')），零 naive-ui 组件名查询（insight L45）。AppLayout.spec mount 范式：setActivePinia(createPinia()) 真 Pinia（app store 默认 binMissing=[] 顶栏横幅整块跳过免 mock downloader）+ vi.mock vue-router useRoute+useRouter + vi.mock naive-ui useMessage + router-view stub + NConfigProvider/NMessageProvider wrap。改动仅 web/src/components/{AppLayout,log/LogList,FirewallHint,PublicIpDetector}.vue + 5 spec + baseline + dev-map，未碰后端/store/路由/API/DB/util。e2e 不受影响（不改任何菜单 label 文案，03-dashboard.spec.ts:11 唯一按 getByText('仪表盘') 文本断言文案不变；insight L34）。0 rollback；GR APPROVED WITH CONDITIONS（8 维全 PASS，C-1 裁定 role=log / C-2/C-3 透传 dev 实测 SA 预批降级 / C-4 ⚒ 唯一 / C-5 baseline bump）；CR APPROVED 一次过（0 CRITICAL/MAJOR/MINOR，2 NIT）；QA APPROVED FOR DELIVERY 0 缺陷；06 含裸 ## Adversarial tests；07 含裸 ## Insight（n-menu icon 无障碍名挂 render span 不依赖 MenuOption 透传 + role=img 防逐字朗读 / 可滚容器 tabindex 落点须与 overflow 元素同元素 + 无需显式 keydown）。verify_all 全量真跑因 PM/dev/QA role-collapsed 上下文无 Bash/PS（insight L31）标 PENDING，静态+确定性预测全绿（预期 PASS / frontend_tests==552 / go_tests==333），交付硬闸门交 batch orchestrator Bash 会话真跑） |
| T-063 | loginfail-kv-purge | DELIVERED（mode: full；批次 ux-ui-uplift-2026-05 第 2 个；partitioned dev-db+dev-backend，无前端；0 rollback；verify_all 全量真跑 PENDING 待 batch orchestrator Bash 会话执行作硬闸门，特别复核 session 既有两测试零回归 + go_tests==333） | 2026-05-31 | `docs/features/loginfail-kv-purge/`（pending archive；按批次约定未 commit/未 archive，由 batch orchestrator 统一处理。对称补齐 T-046 只清 sessions 表、未清 loginfail KV 的资源泄漏另一半：关闭永不复访的轮换源 IP（NAT 后扫描器/攻击者）留下的 `loginfail.<ip>` 限流计数 KV 行永久滞留、无界增长。采用方案设计选项 B（过期语义单点收敛 ratelimit 层，与限流窗口常量同源；storage 只提供机械前缀列举）。dev-db：`internal/storage/kv.go` 新增 `KVEntry{Key,Value}` + `KVListByPrefix(ctx,prefix)([]KVEntry,error)`（`SELECT key,value FROM kv WHERE key LIKE escapeLike(prefix)+'%' ESCAPE '\' ORDER BY key`，QueryContext+rows 迭代参照 proxies.go:39；无匹配返 nil+nil；仅 IO/SQL 真错返 err）+ 未导出 `escapeLike` 转义 LIKE 元字符 `\ % _`（防御未来调用方传含元字符前缀，对当前 loginfail. 行为等价）；所有 SQL 仍只在 storage 红线满足，无 schema/migration。dev-backend：`internal/auth/ratelimit.go` 导出 `LoginFailKeyPrefix='loginfail.'`（`key(ip)` 改用消字面量散落）+ `kvStore` 接口扩 `KVListByPrefix([]storage.KVEntry)`（引入 auth→storage 单向依赖，无环：已核验 storage 不 import auth，admin.go:14 仅注释提及）+ 新增 `RateLimiter.PurgeExpired(ctx)(int,error)`：持 r.mu→KVListByPrefix('loginfail.')→逐条 json.Unmarshal failRecord，过期判定 `now.After(rec.FirstAt.Add(failWindow))` 与 `Allow:74` 字节级同义（删除集 ⊆ 惰性清理集，绝不删窗口内活计数，限流不失效；窗口边界 now==expires 时 After 为 false 不删），损坏 JSON 值视为过期删除（Allow 的 read() 对损坏值同样 ok=false→该 IP 本就放行，删损坏行对限流零影响纯垃圾回收），best-effort 单条 KVDelete 失败不中止累计已删数+返首个错误。`cmd/frp-easy/main.go`：purgeSessionsLoop→purgeLoop 泛化（加 rl 参数，启动即清+每 ticker tick 都跑 purgeExpiredSessionsOnce + 新增 purgeExpiredLoginFailsOnce 两清理，共用同一 goroutine/ticker 不新增 goroutine，随 ctx 取消退出）+ 调用点 :335 改 `go purgeLoop(rootCtx,store,rl,logger)`（rl 已在:270 构造）；sessionPurgeInterval 复用不重命名（OOS-3）。+11 Go 顶层测试（go_tests 322→333 / test_count 856→867 / version 29；dev 9 + QA 独立对抗 2）：storage_test +3（OnlyMatchingPrefix 只命中 loginfail. 升序+近似键 loginfailure.x/loginfail（无点）/xloginfail.y 与 mode.*/system.* 不泄露 AC-2/R-3/BC-6、EmptyResult BC-1、LikeMetacharsAreLiteral 前缀含 _ 当字面不通配 R-3）+ auth_test dev +4（fakeKV 扩 KVListByPrefix 排序对齐；RemovesExpiredKeepsLive 过期删/活留/边界 now==expires 不删+活行清后仍 Allow 阻塞 AC-3/BC-2/3/4/NF-S、CorruptValueRemoved BC-5/R-2、OnlyTouchesLoginfailPrefix 反向证伪 mode.*/system.*/近似键全存活 AC-7c/R-3、EmptyNoop BC-1）+ auth_test QA 独立对抗 +2（SubThresholdExpiredAlsoPurged count<failMax 过期行也清不与 count 耦合反向证伪漏清、DoesNotCorruptSubsequentRateLimit 清后 IP 立刻可 Allow 无残留封禁且 RecordFailure 全新窗口 count=1 无脏状态）+ session_purge_test +2（PurgeExpiredLoginFailsOnce wiring 真 DB over-delete 防御、PurgeLoop_RunsBothPurgesOnStart 启动即触发双清理 poll-until-condition+deadline insight L10+ctx 取消退出 AC-4/IS-2/IS-6）。既有 TestPurgeSessionsLoop_ExitsOnCancel 改调 purgeLoop+传 rl（改名连带机械同步，红线 3 允许 OBS-2）。改动仅 internal/storage/{kv.go,storage_test.go} + internal/auth/{ratelimit.go,auth_test.go} + cmd/frp-easy/{main.go,session_purge_test.go} + baseline + dev-map，未碰前端/API 路由/HTTP handler/migration/DB schema。-race 因本机无 cgo 不跑（与 T-050 先例一致），并发安全静态论证：PurgeExpired 持 r.mu 与 RecordFailure/Allow/Reset 同锁串行化，锁顺序 r.mu 外 s.mu 内无倒置。0 rollback；GR APPROVED FOR DEVELOPMENT（8 维全 PASS）；CR APPROVED 一次过（0 CRITICAL/MAJOR/MINOR，2 NIT）；QA APPROVED FOR DELIVERY 0 缺陷；06 含裸 ## Adversarial tests（AT-1 活计数清后仍限流 / AT-2 过期不靠惰性也清 / AT-3 前缀只匹配 loginfail. 不误删 mode.*/frps 配置/frpc admin creds + QA-ADV-1/2）；07 含裸 ## Insight（资源泄漏修复对称性陷阱：过期语义源不同导致漏建清理通路，正解 storage 机械列举+ratelimit 懂语义单点收敛 + sub-threshold 过期行漏清陷阱）。verify_all 全量真跑因 PM/dev/QA role-collapsed 上下文无 Bash/PS（insight L31）标 PENDING，静态+确定性预测全绿，交付硬闸门交 batch orchestrator Bash 会话真跑） |
| T-062 | onboarding-next-step-guidance | DELIVERED（mode: full；批次 ux-ui-uplift-2026-05 第 1 个；dev-frontend 单分区；verify_all 全量真跑 PENDING 待 orchestrator Bash 会话执行作硬闸门，特别复核 Wizard T-057 既有用例零回归 + frontend_tests==534） | 2026-05-31 | `docs/features/onboarding-next-step-guidance/`（pending archive；按批次约定未 commit/未 archive。补正向 onboarding 引导 + 跨页连通 + Wizard both token 不一致预警，消除"配好了不知道下一步"断点；纯 UI 文案/导航/一处条件比较，不碰后端/store/API/路由守卫/数据流/router.ts、零新依赖。IS-1 Wizard step3 全就绪分支（frpc/both）加「前往代理规则添加端口」按钮 push('/proxies')（frps 不显示 BC-1、缺失分支不加 BC-2），与 T-057 既有全就绪自动跳转/缺失警告/binWarning 定格快照并存不破坏（附加不阻断，AC-11 护栏 + 既有 30+ T-057 用例零回归）。IS-2 Client.vue handleSave 成功 showNextStepHint 显示同款引导按钮，失败不显示（BC-7）。IS-3 Proxies.vue handleSubmit 新增/编辑成功 showPostSaveHint 显示「去仪表盘启动 frpc」+「去服务端监控查看运行态」两入口，删除不触发（BC-6）。IS-4 Proxies #empty n-empty #extra 加「去服务端监控」连通入口（保留既有「点击右上角新增规则」文案不重复）。IS-5 Server.vue loaded 态 #action 加「查看运行态」push('/server/monitor')，与 ServerMonitor goServerConfig→push('/server') 双向连通，加载失败/中态不显示（BC-8）。IS-6 ProxyForm.vue tcp/udp 远程端口字段加 n-text 纯文案「需在服务端端口策略允许范围内」，不读 allowPorts 不联动校验（OOS-1）。IS-7 Wizard step2 both 模式 frps/frpc token trim 后均非空且不等→非阻断 warning，computed tokenMismatch 仅 template v-if 用、handleNext 不引用（不阻止推进、不进 configError，OOS-2）。所有导航 router.push 禁 href（insight L17）；Wizard 顶级路由就地呈现（insight L30/L31）。+34 前端测试（frontend_tests 500→534 / test_count 822→856 / version 28）：Wizard.spec +14（IS-7 5 + IS-1 5 + Adversarial 4：dev 2 token 不一致不阻断/纯空白 trim 不误报 + QA 独立 2 缺失态分支互斥/push 两路径独立共存）+ Client.spec +4 + Proxies.spec +8（IS-3 5 + IS-4 2 + Adversarial 1 失败态不出现空态入口）+ Server.spec +5 + ProxyForm.spec +3。断言全用 DOM 文本 + find('button')按文本 + getExposed + pushSpy（vi.mock vue-router useRouter→push spy，复用 Wizard.spec 既有范式），零 naive-ui 组件名查询（insight L45 / T-057 教训）。import 增量：Client +NAlert、Proxies +NAlert、ProxyForm +NText、Wizard +computed、Server +useRouter。改动仅 web/src/pages/{Wizard,Client,Proxies,Server}.vue + web/src/components/ProxyForm.vue + 对应 5 spec + baseline + dev-map。e2e 不受影响（PM+QA 双核 grep：01-setup/02-auth/03-dashboard 无新引导文案断言、bypassWizard 绕过向导、不进 Proxies/Server/Client 编辑流，insight L34）。0 rollback；GR APPROVED WITH CONDITIONS（R-1 张力不构成需求缺陷不回退）；CR APPROVED；QA APPROVED FOR DELIVERY 0 缺陷；06 含裸 ## Adversarial tests；07 含裸 ## Insight（自动跳转态引导可达性低应锚无跳转成功态 + 给无 useRouter 组件加导航的 spec router mock 范式）。verify_all 全量真跑因 PM/dev/QA 上下文无 Bash/PS 标 PENDING，静态+确定性预测全绿，交付硬闸门交 batch orchestrator Bash 会话真跑） |
| T-061 | clipboard-util-extract | DELIVERED（mode: full；verify_all 全量真跑 PENDING 待 orchestrator Bash 会话执行作硬闸门，特别复核 LogViewer/FirewallHint/PublicIpDetector 三组件既有 spec 零回归 + frontend_tests==500） | 2026-05-30 | `docs/features/clipboard-util-extract/`（pending archive；按用户要求未 commit/未 archive。偿还 T-058 (A) 决策 D1 记录的 backlog：把 LogViewer.vue::onCopy / FirewallHint.vue::copyText / PublicIpDetector.vue::copyText 三处逐字重复的 'navigator.clipboard.writeText→失败回落临时离屏 aria-hidden textarea+select+execCommand(copy)' 逻辑抽到单一纯函数 web/src/utils/clipboard.ts::copyToClipboard(text): Promise<boolean>（首选 writeText resolve→true 不走 fallback；catch→textarea+execCommand 返回其布尔，execCommand 抛错→false，finally removeChild；util 不 import naive-ui、不调 message——useMessage 是组合式 hook 只能在组件 setup 用，故 UI 反馈留各组件层，util 只返回成功布尔）。三组件改 const ok = await copyToClipboard(text) 后按现状逐字调 message（成功 '已复制到剪贴板' / 失败 '复制失败：请手动选择文本复制'）：FirewallHint/PublicIpDetector 的 copyText 体压成 message[ok?'success':'error'](ok?成功:失败); return ok（保留 copyCmd/copyAll/copyIp 对 ok 的 '已复制 ✓' 短暂态依赖）；LogViewer.onCopy 保留 build text（search.visibleLines map raw join）后改调 util，成功/失败各调 message 一次——可观察行为字节不变。纯 1:1 行为搬运无新行为（insight L42）；新增 UI 反馈留组件、util 纯函数可单测（insight 本任务新增第 2 条）。+9 前端测试（frontend_tests 491→500 / test_count 813→822 / version 27）：新建 web/src/utils/__tests__/clipboard.spec.ts——dev 7（首选 resolve→true 且 execCommand 未调用 1 + fallback execCommand true/false/抛错 3 + BC-1 空串 + fallback textarea 持有目标文本并清理 2 + Adversarial 双重失败→false 不外抛+清理 1）+ QA 独立反向证伪 2（writeText reject 时 writeText 恰 1 次 execCommand 恰 1 次不重试不递归 + 连续两次 fallback 结果相互独立无残留状态 false 后 true + 每次清理）。测试模拟 Object.defineProperty(navigator,'clipboard',{value:{writeText:mock},configurable:true}) + 显式装 document.execCommand（happy-dom 默认无）+ 断言纯布尔 + DOM querySelector('textarea[aria-hidden]') 残留检查，零 naive-ui 组件名查询（insight L37/L45）；util 无 UI 不需 naive-ui mock。三组件既有 spec（LogViewer.spec AC-6 只 mock+断言 navigator.clipboard.writeText 不断言 message/fallback DOM；FirewallHint/PublicIpDetector spec mock 命中 util 内部同一全局）零改动零回归——T-058 D1 规避抽取的核心顾虑经 GR/CR/QA 三轮独立核验确认是伪命题（insight 本任务新增第 1 条）。改动仅 web/src/utils/clipboard.ts（新）+ 其 spec（新）+ LogViewer.vue/FirewallHint.vue/PublicIpDetector.vue（各删本地 fallback 块换调 util + import）+ baseline + dev-map（可复用工具表 +1 行），未碰后端/store/路由/API/DB/migration/e2e。e2e 不受影响（insight L34：烟雾测试不点复制按钮、无'复制'断言，T-058 已核实）。0 rollback；CR APPROVED 一次过；06 含裸 ## Adversarial tests（双重失败 / 抽取无重试 / 无残留状态 3 条独立证伪）；07 含裸 ## Insight。verify_all 全量真跑因 PM 上下文无 Bash/PS 标 PENDING，静态+确定性预测全绿，交付硬闸门交 orchestrator Bash 会话真跑，特别复核三组件 spec 零回归 + frontend_tests==500） |
| T-060 | server-reload-dirty-allowports | DELIVERED（mode: full；verify_all 全量真跑 PENDING 待 orchestrator Bash 会话执行作硬闸门；按用户要求未 commit/未 archive） | 2026-05-30 | `docs/features/server-reload-dirty-allowports/`（pending archive；补齐 T-058 (B) 已知局限：让 Server.vue「重新加载」dirty 检测纳入 AllowPortsEditor 端口策略，消除"只改端口策略→点重新加载→无确认→静默丢弃端口编辑"数据丢失路径。新增内联纯函数 normalizeAllowPorts（single→'s:N' / range→'r:A-B' / join('\|')，顺序+形态敏感）+ loadedAllowPortsSnapshot 规范化字符串快照（loadConfig 末尾从 cfg.allowPorts 派生，与 loadedSnapshot 同步刷新）；isDirty() 标量浅比较短路后追加 normalizeAllowPorts(allowPortsEditorRef.value?.getAllowPortsInput() ?? []) !== loadedAllowPortsSnapshot（ref 未挂载退化空策略不抛错）；更新注释移除"已知局限"措辞。双侧同 normalize 函数→round-trip identity（合法加载值未改判非脏）。不改 AllowPortsEditor（保留 T-040 单向数据流，不引 v-model 桥）、不改 Client.vue（frpc 无 allowPorts）、不碰后端/store/路由/util。+10 前端测试（frontend_tests 481→491 / test_count 803→813，version 26）：normalize 稳定性 3 + 端口策略纳入 dirty 6（AC-5 round-trip / AC-1 DOM 加单端口行→脏弹确认不调 apiGet / AC-2 未改直接重载 / AC-6 端口策略侧 confirmReload→apiGet+1+快照刷新 / AC-6 标量侧配套→复位 isDirty 归零 / 改标量仍弹确认）+ Adversarial 1（DOM 点删除删一行端口→isDirty 捕获→弹确认不静默重载，反向证伪 T-058 局限）。断言全用 DOM 按钮文本（'添加单端口'/'删除'，真 AllowPortsEditor 未 mock）+ getExposed，零 naive-ui 组件名查询（insight L45 / T-057）。1 rollback（dev-frontend：QA 对抗发现 D-1——AC-6 原断言"重载后 isDirty 归零"与 AllowPortsEditor 单向数据流不复位 props.initial 范式冲突会 FAIL，纯测试侧拆为端口策略侧+标量侧双用例修复，零生产逻辑变更）；CR/QA 复审均 APPROVED；06 含裸 ## Adversarial tests；07 含裸 ## Insight（dirty 纳入独立子编辑器的 round-trip 快照范式 + 子编辑器不复位的测试 AC 张力）。verify_all 全量真跑因 PM 上下文无 Bash/PS（insight L31）标 PENDING，静态+确定性预测全绿，交付硬闸门交 orchestrator Bash 会话真跑） |
| T-059 | proxy-remoteport-conflict-sentinel | DELIVERED（mode: full；verify_all 全量真跑 PENDING 待 orchestrator Bash 会话执行作硬闸门） | 2026-05-30 | `docs/features/proxy-remoteport-conflict-sentinel/`（pending archive；偿还 T-055 backlog：把 (type,remote_port) 唯一冲突在 storage 层 sentinel 化，消除 handler 对 SQL 驱动错误文本的脆弱 strings.Contains 匹配，对称复刻 ErrDuplicateName/isDuplicateNameError 范式。storage：新增 store.go ErrDuplicateRemotePort + proxies.go isDuplicateRemotePortError 助手（匹配 UNIQUE constraint failed + proxies.remote_port，与 name 版互斥），UpsertProxy insert+update 两处在 isDuplicateNameError 后加 isDuplicateRemotePortError→ErrDuplicateRemotePort 再 fallthrough 原 fmt.Errorf；SQL 文本匹配只留 storage 层。handler mapProxyWriteError：ErrDuplicateName 409 分支后加 errors.Is(ErrDuplicateRemotePort)→422+CONFLICT+field=remotePort+固定中文'该类型下远程端口已被占用，请改用其它端口'；删除原 unique/constraint/remote_port strings.Contains 整块；validation 块（requires/must not/invalid）面向前端 msg 由透传 storage 英文改固定中文'代理配置校验失败'+原始 error 进 logger.Warn（不泄露内部文本，延续 T-055 writeInternalError 原则）。+4 Go 顶层测试（go_tests 318→322 / test_count 799→803）：storage +3（UpdateToDuplicateRemotePortReturnsSentinel / IsDuplicateRemotePortError_DirectChecks 正负互斥表驱动 / Adversarial_T059_RemotePortErrorTextVariants 文本变体）+ handler +1（MapProxyWriteError_DuplicateRemotePort 422+field+不泄露 SQL 英文）；另改名 2（DuplicateTypeRemotePortNotSentinel→ReturnsSentinel 弱断言升正向、Validation_Preserved→FixedMessage_NoLeak C-1 红线3 例外经 PM 批准因有意改变行为）+ 强化既有断言 3 处（真 DB p3 / 端到端 message 不含英文）。改动仅 internal/storage/{store,proxies,proxies_test,qa_t007_adversarial}.go + internal/httpapi/{handlers_proxies,handlers_hygiene_test,handlers_proxies_test}.go + baseline + dev-map，不改已合并 migration（0001）、不碰前端/路由/util。0 rollback；CR APPROVED 一次过；06 含裸 ## Adversarial tests（AT-1 驱动错误文本变化也不影响 handler 分类——sentinel 后 handler 已无 strings.Contains，文本依赖单点收敛 storage + 直测护栏锁死，反向论证）；verify_all 全量真跑因 PM 上下文无 Bash/PS 标 PENDING，静态确定性闸门全绿，交付硬闸门交 orchestrator Bash 会话真跑） |
| T-058 | frontend-interaction-polish | DELIVERED（mode: full；verify_all 全量真跑 PENDING 待 orchestrator Bash 会话执行作硬闸门） | 2026-05-30 | `docs/features/frontend-interaction-polish/`（pending archive；三项前端交互一致性小修：(A) 剪贴板写入失败不再静默——FirewallHint copyCmd/copyAll + PublicIpDetector copyIp 1:1 内联 LogViewer onCopy 范式（clipboard→message.success / catch textarea+execCommand fallback → 成功 success / 失败 message.error '复制失败：请手动选择文本复制'；保留'已复制 ✓'仅成功触发），引入 useMessage；决策 D1 不抽 utils/clipboard.ts（避免改 LogViewer.vue 扩散+动其测试快照，记 backlog）。(B) Server/Client'重置'→'重新加载' + dirty 防误丢：加载存标量快照 loadedSnapshot，handleReloadClick 时 isDirty()（浅比较 6/3 标量字段）则弹 ConfirmDialog（复用 T-056）确认才 reload，不 dirty 直接 reload；决策 D2 dirty 不覆盖 AllowPortsEditor 子组件（单向数据流 insight L13，已知局限：仅 allowPorts 改动退化为直接重载）。(C) Wizard step2 frpc 标题死分支清理（原 v-if='both'/v-else 两分支文案相同 → 合并单个无条件 n-text，零行为变化）。+27 前端测试 frontend_tests 454→481 / test_count 772→799（FirewallHint.spec 新建 7 + PublicIpDetector.spec +4 + Server.spec +7 + Client.spec +6 + Wizard.spec +3）；断言全用 DOM/文本/getExposed，零 naive-ui 组件名查询（insight L45 / T-057 教训），message 用 vi.mock('naive-ui') 单例 spy + clipboard mock + execCommand mock。改动仅 5 SFC + 5 spec + baseline + dev-map，未碰后端/store/路由/util。e2e 不受影响（01-setup/02-auth/03-dashboard 无任何'重置'/'重新加载'/'frpc 客户端配置'/'复制'断言，03 用 bypassWizard 绕过向导）。0 rollback；CR APPROVED 一次过；06 含裸 ## Adversarial tests（clipboard reject + execCommand 失败 → message.error 反向证伪）；verify_all 全量真跑因 PM 上下文无 Bash/PS 标 PENDING，静态+设计保真度全绿，交付硬闸门交 orchestrator Bash 会话真跑） |
| T-057 | binary-missing-onboarding-ux | DELIVERED（mode: full；verify_all 真跑 PENDING 待 orchestrator Bash 会话执行作硬闸门） | 2026-05-30 | `docs/features/binary-missing-onboarding-ux/`（pending archive；改善二进制缺失首用体验两处：(A) Dashboard 缺失提示 n-alert 文案对齐 AppLayout 顶栏横幅信息架构——首选引导「一键下载/手动上传」、手动放置目录退为兜底，alert 内不重复造按钮；(B) Wizard 完成保存配置+开启自动启动后、跳转前 await appStore.fetchReady() 刷新 binMissing，按所选角色 missingForRole 求缺失交集：缺失则不自动跳走、不发「正在跳转」toast、step3 就地 warning + 「进入仪表盘」手动按钮；不缺失维持原自动跳转。binWarning 用 ref 定格快照。改动仅 Dashboard.vue+Wizard.vue+2 spec+baseline+dev-map，未碰后端/store/路由守卫/AppLayout；e2e 不受影响（01-setup 仅断言离开/setup、03-dashboard 用 bypassWizard 绕过向导）；0 rollback；+17 前端测试 frontend_tests 437→454 / test_count 755→772；CR APPROVED 一次过；06 含裸 ## Adversarial tests（both 选中 frps 缺失→警告出现未静默跳 反向证伪）；verify_all 全量真跑因 PM 上下文无 Bash/PS 标 PENDING，静态+设计保真度全绿，交付硬闸门交 orchestrator Bash 会话真跑） |
| T-056 | proc-stop-destructive-confirm | DELIVERED（mode: full；verify_all 真跑 PENDING 待用户/Bash 会话执行作硬闸门） | 2026-05-30 | `docs/features/proc-stop-destructive-confirm/`（pending archive；Dashboard 停止/重启破坏性操作加二次确认，复用 ConfirmDialog + pendingAction 状态机驱动动态文案，启动不确认；改动仅 Dashboard.vue + 其 spec + baseline + dev-map；0 rollback；+11 前端测试 frontend_tests 426→437 / test_count 744→755；CR APPROVED 一次过；verify_all 全量真跑因 PM 上下文无 Bash/PS 标 PENDING，静态预测全绿，交付硬闸门交用户/orchestrator Bash 会话真跑） |
| T-055 | backend-api-hygiene | DELIVERED | 2026-05-30 | `docs/features/backend-api-hygiene/`（pending archive；batch project-optimization-2026-05；A: frpsadmin client Proxies/ProxyDetail/Traffic 三处 path 加 url.PathEscape 防注入根防御 + serverRuntimeProxyDetail type 白名单校验前移→422；B: 抽 writeInternalError helper 统一 3 处 500 兜底（procStop/mapProxyWriteError/downloadBin）固定文案 + 原始 error 进 logger，保留 ErrDuplicateName 409 / unique 422 / validation 透传 / uploadBin errno；+10 Go 测试 go_tests 308→318 / test_count 734→744；0 rollback；verify_all 由 orchestrator 真跑硬闸门） |
| T-054 | archive-task-sh-regex-align | DELIVERED | 2026-05-30 | `docs/features/archive-task-sh-regex-align/`（pending archive；batch project-optimization-2026-05；修 archive-task.sh:50 awk Insight 标题正则→容错单 token 前缀 `([^[:space:]]+[[:space:]]+)?`，对齐 .ps1:48，偿还 T-028 遗留双实现不对称债；改动面 1 文件；无新增测试；verify_all 由 orchestrator 真跑硬闸门） |
| T-049 | docs-contract-drift-fix | DELIVERED | 2026-05-30 | `docs/features/docs-contract-drift-fix/`（pending archive；batch project-optimization-2026-05；openapi 补 service-status 路由+schema（30 path）+ dev-map 树补 svcprobe/utils/test-utils + 修路由计数 + 2 HTML 加时效声明；verify_all PASS 32/0/0） |
| T-051 | frontend-test-coverage | DELIVERED | 2026-05-30 | `docs/features/frontend-test-coverage/`（pending archive；batch project-optimization-2026-05；proxies/wizard store + useProxyForm + statusUtils/useLogLevelFilter/useServiceStatus + api/client.ts +84 测试；frontend_tests 342→426；verify_all PASS 32/0/0） |
| T-053 | autorestore-canceled-persist-fix | DELIVERED | 2026-05-30 | `docs/features/autorestore-canceled-persist-fix/`（pending archive；batch project-optimization-2026-05；修 retryRestoreLoop canceled 分支用已取消 ctx 持久化→canceled outcome 落不进 kv；改 detached ctx + 升级测试正向断言；verify_all PASS 32/0/0） |
| T-050 | backend-test-coverage | DELIVERED | 2026-05-30 | `docs/features/backend-test-coverage/`（pending archive；batch project-optimization-2026-05；validate.go/procmgr 终态断言/autoRestore/svcprobe/procmgr 子进程生命周期 +21 测试；go_tests 287→308；发现 retryRestoreLoop canceled-persist bug→T-053；verify_all PASS 32/0/0） |
| T-048 | frontend-consistency-cleanup | DELIVERED | 2026-05-30 | `docs/features/frontend-consistency-cleanup/`（pending archive；batch project-optimization-2026-05；删重复 formatBytes + formatTime 本地化统一 + 可读语义色 + router.push + 响应式 + extractErrorMessage + 进程文案统一；frontend_tests 327→342；verify_all PASS 32/0/0） |
| T-047 | frontend-honest-states | DELIVERED | 2026-05-30 | `docs/features/frontend-honest-states/`（pending archive；batch project-optimization-2026-05；Server/Client 加载+错误三态 + Proxies 失败/空区分 + Dashboard 开关不静默 + Server dashboard 校验；frontend_tests 297→327；verify_all PASS 32/0/0） |
| T-046 | session-purge-and-requestid | DELIVERED | 2026-05-30 | `docs/features/session-purge-and-requestid/`（pending archive；batch project-optimization-2026-05；过期 session 定时清理 loop + RequestID crypto/rand + 3 新测试；go_tests 284→287；verify_all PASS 32/0/0） |
| T-045 | backend-deadcode-cleanup | DELIVERED | 2026-05-30 | `docs/features/backend-deadcode-cleanup/`（pending archive；batch project-optimization-2026-05；删 procmgr 无人订阅发布订阅+死函数+var _ hack 净~90 行；go_tests 285→284；verify_all PASS 32/0/0） |
| T-052 | e2e-decouple-port | DELIVERED | 2026-05-30 | `docs/features/e2e-decouple-port/`（pending archive；batch project-optimization-2026-05；e2e 改独立端口 17800 + webServer.env 注入，根治 C.1 假性失败；全量 verify_all 首次本机全绿 PASS 32/0/0） |
| T-044 | verify-gate-hardening | DELIVERED | 2026-05-30 | `docs/features/verify-gate-hardening/`（pending archive；batch project-optimization-2026-05；.ps1 B.3 真查退出码 + B.4 双实现真计数 + baseline 刷新 285/297/582 + E.6 三报告标题归一化；基线回 PASS 31/0/0） |
| T-043 | frontend-test-suite-repair | DELIVERED | 2026-05-30 | `docs/features/frontend-test-suite-repair/`（pending archive；batch project-optimization-2026-05；修 39 个前端测试失败恢复 B.3 绿，新增 getExposed/apiError test-utils） |
| T-042 | proxy-runtime-status-merge | DELIVERED | 2026-05-28 | `docs/features/proxy-runtime-status-merge/`（pending archive） |
| T-041 | server-monitor-page-ui | DELIVERED | 2026-05-28 | `docs/features/server-monitor-page-ui/`（pending archive） |
| T-040 | frps-allow-ports-policy | DELIVERED | 2026-05-27 | `docs/features/frps-allow-ports-policy/`（pending archive） |
| T-039 | frpsadmin-server-runtime-api | DELIVERED | 2026-05-27 | `docs/features/_archived/frpsadmin-server-runtime-api/` |
| T-038 | boot-autostart-hardening | DELIVERED | 2026-05-25 | `docs/features/_archived/boot-autostart-hardening/` |
| T-036 | log-ui-ux-polish | DELIVERED | 2026-05-24 | `docs/features/_archived/log-ui-ux-polish/` |
| T-037 | proxy-rules-simplify-and-port-fix | DELIVERED | 2026-05-24 | `docs/features/_archived/proxy-rules-simplify-and-port-fix/` |
| T-035 | install-sh-role-cli-arg-passthrough | DELIVERED | 2026-05-24 | `docs/features/_archived/install-sh-role-cli-arg-passthrough/` |
| T-033 | e2e-setup-spec-flake-fix | DELIVERED | 2026-05-24 | `docs/features/_archived/e2e-setup-spec-flake-fix/` |
| T-034 | reviewer-write-tool-dispatch-verify | DELIVERED | 2026-05-24 | `docs/features/_archived/reviewer-write-tool-dispatch-verify/` |
| T-032 | proxy-form-vmodel-oom-fix | DELIVERED | 2026-05-24 | `docs/features/_archived/proxy-form-vmodel-oom-fix/` |
| T-031 | install-ps1-host-close-on-completion | DELIVERED | 2026-05-24 | `docs/features/_archived/install-ps1-host-close-on-completion/` |
| T-030 | reviewer-agent-write-tool-frontmatter | DELIVERED | 2026-05-24 | trivial 直接修复（.harness/agents/gate-reviewer.md + code-reviewer.md frontmatter `tools` 字段 + harness-sync 同步 .claude/agents/，无阶段文档）：insight L41/L48 + T-027 第 3 次复现"reviewer 不落盘"陷阱（200+ 行 Markdown 塞消息体让 PM 代写），根因是 frontmatter tools 仅 Read/Glob/Grep；加 Write 工具与 developer/qa-tester 对齐 |
| T-029 | readme-ps51-zhcn-disk-form-warning | DELIVERED | 2026-05-24 | trivial 直接修复（README.md +1 段，无阶段文档）：T-026 删 install.ps1 BOM 后 PS5.1+zh-CN 磁盘形态会显示中文乱码（OOS-9 接受），README 一键安装 Windows 段加引导块明示"iex 形态首选；磁盘形态请用 PS7 跑 pwsh 或保持 iex" |
| T-028 | archive-task-insight-regex-tolerance | DELIVERED | 2026-05-24 | trivial 直接修复（archive-task.ps1 ~6 行，无阶段文档）：07 §N Insight 数字编号前缀让 regex 不命中，T-019/T-021/T-024/T-026 4 次复现；改 regex `^##\s+(?:[^\s\n]+\s+)?Insights?\s*$` 容忍可选前缀 token；N=0 时显式 Write-Warning 不静默 |
| T-026 | install-ps1-iex-bom-and-host-exit-fix | DELIVERED | 2026-05-24 | `docs/features/_archived/install-ps1-iex-bom-and-host-exit-fix/` |
| T-027 | download-cancel-and-upload-decouple | DELIVERED | 2026-05-24 | `docs/features/_archived/download-cancel-and-upload-decouple/` |
| T-025 | download-bin-timeout-fix | DELIVERED | 2026-05-23 | `docs/features/_archived/download-bin-timeout-fix/` |
| T-024 | install-ps1-iex-cmdletbinding-fix | DELIVERED | 2026-05-23 | trivial 直接修复（install.ps1 删 `[CmdletBinding()]` 一行 + 注释，无阶段文档）：iex inline 不允许该 attribute，会触发 "Unexpected attribute 'CmdletBinding'"；`param([switch]$Help)` 保留兼容磁盘形态 |
| T-023 | upload-bin-content-type-fix | DELIVERED | 2026-05-23 | trivial 直接修复（system.ts + spec.ts，无阶段文档）：apiClient 实例 default `Content-Type: application/json` 污染 FormData 请求让 axios 不再自动补 multipart boundary，显式 `headers: { 'Content-Type': undefined }` 抵消 |
| T-022 | service-mode-stderr-bridge | DELIVERED | 2026-05-23 | trivial 直接修复（main.go 单文件 ~10 行 + 2 个单测，无阶段文档）：exposureNotice 改走 logger，让服务模式 ui.log 也能拿到提示 |
| T-021 | encoding-ps51-bom | DELIVERED | 2026-05-23 | `docs/features/_archived/encoding-ps51-bom/` |
| T-019 | windows-service-scm-1053-fix | DELIVERED | 2026-05-23 | `docs/features/_archived/windows-service-scm-1053-fix/` |
| T-020 | claude-settings-context7-fix | DELIVERED | 2026-05-23 | `docs/features/_archived/claude-settings-context7-fix/` |
| T-018 | upload-bin-multiport-ip-probe | DELIVERED | 2026-05-23 | `docs/features/_archived/upload-bin-multiport-ip-probe/` |
| T-017 | install-role-and-public-ip | DELIVERED | 2026-05-23 | `docs/features/_archived/install-role-and-public-ip/` |
| T-016 | install-progress-and-systemd-unit-fix | DELIVERED | 2026-05-23 | `docs/features/_archived/install-progress-and-systemd-unit-fix/` |
| T-015 | fix-dirty-release-version | DELIVERED | 2026-05-22 | trivial 直接修复（单行级，无阶段文档）：`npm install --frozen-lockfile`→`npm ci` |
| T-014 | frp-binary-auto-download | DELIVERED | 2026-05-22 | `docs/features/_archived/frp-binary-auto-download/` |
| T-013 | rolling-release-install | DELIVERED | 2026-05-22 | `docs/features/_archived/rolling-release-install/` |
| T-012 | one-click-install-and-mit-license | DELIVERED | 2026-05-22 | `docs/features/_archived/one-click-install-and-mit-license/` |
| T-011 | readme-refresh-and-network-defaults | DELIVERED | 2026-05-21 | `docs/features/_archived/readme-refresh-and-network-defaults/` |
| T-010 | deploy-polish-and-ci | DELIVERED | 2026-05-19 | `docs/features/_archived/deploy-polish-and-ci/` |
| T-009 | polish-pass | DELIVERED | 2026-05-19 | `docs/features/_archived/polish-pass/` |
| T-008 | deploy-kit | DELIVERED | 2026-05-19 | `docs/features/_archived/deploy-kit/` |
| T-007 | hardening-pass-audit | DELIVERED | 2026-05-19 | `docs/features/_archived/hardening-pass-audit/` |
| T-006 | e2e-smoke-tests | DELIVERED | 2026-05-17 | `docs/features/_archived/e2e-smoke-tests/` |
| T-005 | docs-and-api-schema | DELIVERED | 2026-05-16 | `docs/features/_archived/docs-and-api-schema/` |
| T-004 | tech-debt-cleanup | DELIVERED | 2026-05-16 | `docs/features/_archived/tech-debt-cleanup/` |
| T-003 | readme-and-health-report | DELIVERED | 2026-05-16 | `docs/features/_archived/readme-and-health-report/` |
| T-002 | zero-config-quickstart | DELIVERED | 2026-05-16 | `docs/features/_archived/zero-config-quickstart/` |
| T-001 | web-ui-mvp | DELIVERED | 2026-05-16 | `docs/features/_archived/web-ui-mvp/` |

## 约定

- **ID** 顺序编号：`T-001`、`T-002`、...
- **Slug** 小写连字符，≤40 字符（例：`csv-export-orders`）。
- **阶段** 之一：`req`（需求）、`design`（方案）、`gate`（闸门）、`dev`（开发）、`review`（评审）、`test`（测试）、`delivery`（交付）、`blocked`（阻塞）、`done`（完成）。
- **文档目录** 是 `docs/features/<slug>/` 下的相对路径。

## 任务怎么关联

新任务开始时，Requirement Analyst 会扫描这个看板找相关历史：

- 同一模块 → 先读之前的 `02_SOLUTION_DESIGN.md`。
- 同一 feature → 在之前的方案基础上扩展，**不要重新设计**。
- 决策冲突 → 标记给用户。
